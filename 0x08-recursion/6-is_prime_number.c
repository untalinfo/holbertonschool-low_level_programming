#include "holberton.h"

/**
 * is_prime_number - function that input is a prime number
 * @n:  number
 * Return: 1 if prime number otherwise return 0
 */
int is_prime_number(int n)
{
	if (n > 0)
		return (is_prime_number )
	else
		return (0)
}

