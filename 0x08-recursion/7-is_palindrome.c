#include "holberton.h"

/**
 * is_palindrome - function that input is a prime number
 * @s:  pointer to array
 * Return: 1 if a string palindrome otherwise return 0
 */
int is_palindrome(char *s)
{
	if (n > 0)
		return (is_prime_number )
	else
		return (0)
}

